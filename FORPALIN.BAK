#include<stdio.h>
#include<conio.h>
void main()
{
	int i,n,sum,r;
	clrscr();
	for(i=10;i<=1000;i++)
	{
		n=i;
		sum=0;
		while(n>0)
		{
			r=n%10;
			sum=sum*10+r;
			n /=10;
		}
		if(sum == i)
		{
			printf("%d\t",i);
		}
	}
	getch();
}