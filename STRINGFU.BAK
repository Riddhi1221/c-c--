#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
	char c[50];
	char d[50];
	int email=0;
	clrscr();
	printf("Enter=>");
	gets(c);
	printf("Enter=>");
	gets(d);
	/*
	printf("len: %d\n",strlen(c));
	printf("upp: %s\n",strupr(c));
	printf("lwr: %s\n",strlwr(c));
	printf("rev: %s\n",strrev(c));
	 */
	if(strcmp(c,d)==email)
	{
		printf("equal");
	}
	else
	{
		printf("diff");
	}
	getch();
}
