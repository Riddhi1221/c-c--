#include<stdio.h>
#include<conio.h>
void main();
{
	char a[50],i;
	int n;
	clrscr();
	printf("Enter string");
	gets(a);
	puts(a);
	for(i=0;a[i]!='\0';i++)
	{
		printf("\n a[%d]=%c",i,a[i]);
	}
	for(i=0;a[i]!='\0';i++)
	{
		n++;
	}
	printf("\n total char:= %d",n);
	getch();
}